# Agent Guidelines

## Git Configuration

- Use `arthur.theuer@outlook.com` as the commit author email.
- Always push to `origin main` immediately after a successful build.
- The remote URL already contains a PAT for authentication — no additional token setup is needed.
